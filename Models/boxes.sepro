# This is a box
#

def tag box
def tag lid

react bind
    where (box !neck) on (lid !used)
    in left
        bind neck to other
    
    in right
        set used

world main
    10 (box)
    2 (lid)


